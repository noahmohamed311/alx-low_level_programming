#include "main.h"
/**
 * _abs - entry point
 *
 * Description: Prints the alphabet with _putchar
 * @n: character to check
 * Return: void
 */

int _abs(int n)
{
	if (n > 0)
	return (n);
	else if (n < 0)
	return (-n);
return (0);
}
