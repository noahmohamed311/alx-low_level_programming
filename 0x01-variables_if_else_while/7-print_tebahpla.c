#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ABC;
for (ABC = 'z'; ABC >= 'a'; ABC--)
{
	putchar (ABC);
}
	putchar ('\n');
return (0);
}
